# RetroTaskRPGCloud
Same as the other RetroTaskRPG does, but the savegame should be in the cloud and the application runs via streamlit in your browser. So you can check your "adventures" also on your mobile. 
